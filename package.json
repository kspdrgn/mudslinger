{
  "name": "mudslinger",
  "type": "module",
  "watch": {
    "build-client": {
      "patterns": [
        "ts"
      ],
      "extensions": "ts",
      "quiet": false
    },
    "start": "{ts}/*.js"
  },
  "scripts": {
    "prestart": "node tools/prestart.js",
    "start": "node --es-module-specifier-resolution=node dist/server/app.js",
    "postinstall": "node tools/postinstall.js",
    "clean": "rimraf ts/build && rimraf dist && rimraf static/mudslinger.js",
    "build": "npm run build-client && npm run build-server",
    "build-client": "tsc -p ts/tsconfig.client.json && rollup -c ts/rollup.config.js",
    "build-server": "tsc -p ts/tsconfig.server.json",
    "lint": "tslint -c ts/tslint.json ts/src/client/*.ts && tslint -c ts/tslint.json ts/src/server/*.ts",
    "watch": "npm-watch"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/fs-extra": "^11.0.4",
    "@types/jquery": "^3.5.31",
    "@types/node": "22.7.4",
    "@types/ws": "^8.5.12",
    "codemirror": "^6.0.1",
    "fs-extra": "^11.2.0",
    "jquery": "3.7.1",
    "jqwidgets-framework": "^20.0.0",
    "rimraf": "^6.0.1",
    "rollup": "^0.41.4",
    "socket.io": "^4.8.0",
    "socket.io-client": "^4.8.0",
    "tslint": "^6.1.3",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "express": "^4.14.0",
    "npm-watch": "^0.13.0"
  }
}
